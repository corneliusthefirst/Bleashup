import React, { Component } from "react";
import { PulseIndicator } from "react-native-indicators";

export default class updateStateIndicator extends Component {
  render() {
    return <PulseIndicator size={18} color="#1FABAB" />;
  }
}
